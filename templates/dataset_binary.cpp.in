// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (c) 2021 Scipp contributors (https://github.com/scipp)
/// @file
/// @author Simon Heybrock
#include "scipp/variable/@OPNAME@.h"
#include "scipp/dataset/@OPNAME@.h"

#include "dataset_operations_common.h"

namespace scipp::dataset {

DataArray @NAME@(const DataArrayConstView &a, const DataArrayConstView &b) {
  return DataArray(
      @NAME@(a.data(), b.data()),
      union_(a.coords(), b.coords()),
      union_or(a.masks(), b.masks()),
      intersection(a.attrs(), b.attrs()));
}

DataArray @NAME@(const DataArrayConstView &a, const VariableConstView &b) {
  return DataArray(@NAME@(a.data(), b), a.coords(), a.masks(), a.attrs());
}

DataArray @NAME@(const VariableConstView &a, const DataArrayConstView &b) {
  return DataArray(@NAME@(a, b.data()), b.coords(), b.masks(), b.attrs());
}

} // namespace scipp::dataset
