parameters:
- name: verbose
  displayName: Verbose
  type: boolean
  default: false

trigger:
  - main

pr: none

extends:
  template: templates/stages.yml
  parameters:
    verbose: ${{ parameters.verbose }}
    py_versions:
      linux: ['3.7']
      osx: ['3.7']
      windows: ['3.7']
    deploy: true
    conda_label: 'dev'
    publish_docs: true
