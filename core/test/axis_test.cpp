// SPDX-License-Identifier: GPL-3.0-or-later
// Copyright (c) 2020 Scipp contributors (https://github.com/scipp)
#include <gtest/gtest.h>

//#include "scipp/core/axis.h"

// using namespace scipp;
// using namespace scipp::core;

TEST(AxisTest, basics) {
  // const auto var = makeVariable<double>(Dims{Dim::X}, Shape{4});
  // next::DatasetAxis axis(var);
  // axis.unaligned().set("a", var);
  // const auto &unaligned = axis.unaligned();
  // EXPECT_EQ(unaligned.size(), 1);

  // next::Dataset d;
  // coords(d).set(Dim::X, var);
  // EXPECT_EQ(coords(d).size(), 1);
  // auto c = coords(d);
  // EXPECT_EQ(c[Dim::X].data(), var);
  // c[Dim::X].unaligned().set("a", var);
}
