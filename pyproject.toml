[build-system]
requires = [
  "setuptools>=42",
  "wheel",
  "cmake>=3.18",
  "conan",
  "ninja",
  "scikit-build",
  "tbb-devel",
]
build-backend = "setuptools.build_meta"

[tool.cibuildwheel]
archs = ["auto64"]
before-build = "rm -rf {project}/build"  # probably https://github.com/pypa/setuptools/issues/2912
test-requires = "pytest h5py matplotlib xarray pandas scipy>=1.7.0 pythreejs"
test-command = "pytest {project}/tests"
